services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\Tests\Behat\ApiContext:
        arguments:
            $entityManager: '@doctrine.orm.entity_manager'
            $client: '@test.client'

    App\Tests\Behat\CityFixtureContext:
        arguments:
            $entityManager: '@doctrine.orm.entity_manager'
